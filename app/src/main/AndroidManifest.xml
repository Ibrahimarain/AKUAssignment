<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aku.projectassignment">

    <application
        android:name=".AKUApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AKUProjectAssignment">
        <activity android:name=".ui.summary.DashboardActivity"></activity>
        <activity android:name=".ui.residentlist.ResidentListActivity" />
        <activity
            android:name=".ui.survey.ResidentQuestionsActivity"
            android:parentActivityName=".ui.survey.AddResidentActivity" />
        <activity
            android:name=".ui.locality.LocalityActivity"
            android:parentActivityName=".ui.home.HomeActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity
            android:name=".ui.survey.AddResidentActivity"
            android:parentActivityName=".ui.home.HomeActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>